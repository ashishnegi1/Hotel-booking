extends layout
include mixins/_hotel_form 

block content
    a(href="/admin")
        button(type="button" class="button_small") Back to Admin
    h2= title
    +hotelForm(hotel)
    
        
